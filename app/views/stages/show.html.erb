<h1><%=h @stage.title %></h1>

<div id='census_answers'>
	<%= render :partial => "question", :collection => @questions %>
</div>

<div id='prev_next_links'>
	<%= render :partial => "prev_next_stage", :locals => { :prev_stage => @prev_stage, :next_stage => @next_stage  } %>
</div>
