%h2 Listing nominations
  
- for nomination in @nominations 
  %p= link_to "#{nomination.first_name} was nominated by #{nomination.your_name}" , nomination_path(nomination)  
  %br 

= will_paginate @nominations
